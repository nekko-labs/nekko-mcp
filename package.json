{
  "name": "nekko-mcp",
  "private": true,
  "version": "0.2.0",
  "description": "Local-first runtime & manager for MCP servers — run servers securely (process sandbox or Docker), supervise them, and expose one gateway endpoint for any agent harness. A ToolHive rival you own.",
  "license": "MIT",
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/daemon",
    "apps/web"
  ],
  "scripts": {
    "build": "npm run build -w @nekko-mcp/shared && npm run build -w @nekko-mcp/core && npm run build -w @nekko-mcp/daemon && npm run build -w @nekko-mcp/web",
    "test": "npm run test -w @nekko-mcp/core",
    "dev": "node scripts/dev-all.mjs",
    "dev:daemon": "npm run dev -w @nekko-mcp/daemon",
    "dev:web": "npm run dev -w @nekko-mcp/web",
    "daemon": "node --experimental-strip-types apps/daemon/src/index.ts",
    "smoke:http": "node scripts/smoke-http.mjs",
    "typecheck": "tsc -b --noEmit || true"
  },
  "engines": {
    "node": ">=20"
  }
}
